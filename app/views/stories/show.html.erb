<h1><%= @story.title %></h1>
<% if @story.comments.count == 0 %>
<p>No comments posted on this story</p>
<% else %>
<h2>All comments</h2>
<ul>
  <% @story.comments.each do | comment | %>
  <li><%= comment.body %> </br> by: <%= comment.author %></li>
  <% end %>
</ul>
<% end %>

<p> <%= link_to "Back to stories list", stories_path %> </p>
